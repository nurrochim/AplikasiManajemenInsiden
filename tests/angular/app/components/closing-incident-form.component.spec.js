ngDescribe({
    name: 'Test closing-incident-form component',
    modules: 'app',
    element: '<closing-incident-form></closing-incident-form>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
